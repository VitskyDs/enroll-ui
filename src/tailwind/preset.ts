/**
 * Tailwind v4 preset for @vitskyds/enroll-ui.
 *
 * Usage in tailwind.config.ts (if using @config):
 *   import preset from '@vitskyds/enroll-ui/tailwind';
 *   export default { presets: [preset] };
 *
 * For pure CSS-based Tailwind v4 setups, importing @vitskyds/enroll-ui/styles
 * in your root CSS is sufficient — no JS preset needed.
 */
const preset = {
  plugins: [],
}

export default preset
